@page "{Id:int}"
@using Trackify.Domain.Models;
@model Trackify.Pages.User.ProfileModel
@{
}



    <!-- profile pic and info -->
<div class="col-9 flex-grow-1 p-0" style="height: 696px;">
    <div class="card h-100 p-5" style="background: linear-gradient(to bottom, @Model.user.color 0%, #121212 25%);height: 696px;">
            <div class="row p-0 m-0">
                @{
                    if (HttpContext.Session.GetInt32("Id") == Model.user.userId)
                    {
                        <div type="button" class="p-0 me-5" data-bs-toggle="modal" data-bs-target="#EditProfileModal" style="height: 200px; width: 200px;">
                            <img src="@Model.user.pfp" style="height: 200px; width: 200px;" class="roundbutton" />
                        </div>
                    }
                    else
                    {
                        <div class="p-0 me-5" style="height: 200px; width: 200px;">
                            <img src="@Model.user.pfp" style="height: 200px; width: 200px;" class="roundbutton" />
                        </div>
                    }
                }

                <div class="col-6 p-0">
                    <br />
                    <span class="small">Profile</span>
                    <br />
                    @{
                        if (HttpContext.Session.GetInt32("Id") == Model.user.userId)
                        {
                            <div type="button" data-bs-toggle="modal" data-bs-target="#EditProfileModal">
                                <h1 class="fw-bold" style="font-size: 6em; text-overflow: ellipsis; white-space: nowrap; overflow: hidden; width: 1000px">@Model.user.nickname</h1>
                            </div>
                        }
                        else
                        {
                            <h1 class="fw-bold" style="font-size: 6em; text-overflow: ellipsis; white-space: nowrap ;overflow: hidden; width: 1000px;">@Model.user.nickname</h1>
                        }
                    }
                    <span class="small">@Model.userPlaylists.Count public playlists • </span>
                </div>
            </div>
            <div class="row gap-2 pt-5">
            <span class="fw-semibold h3">Playlists</span>
            @{
                foreach (Playlists playlist in Model.userPlaylists)
                {
                    <div class="card pt-2" style="height:220px; width:180px">
                        <a class="text-reset" style="width: 157px" id="clickable-link" asp-page="/User/Playlist" asp-route-id="@playlist.PlaylistId">
                            <img src="@playlist.PlaylistImage" height="155" width="155" />
                            <p class="m-0" style="text-overflow: ellipsis; white-space: nowrap; overflow: hidden; width: 157px">
                                <span class="h3">@playlist.PlaylistName</span>
                            </p>
                        </a>
                    </div>
                }
                //fix image size and don't allow the thing to expand
            } 
            </div>
        </div>
    </div>

<!-- Modal -->
<div class="modal fade" id="EditProfileModal" tabindex="-1" aria-labelledby="EditProfileModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h1 class="modal-title fs-5" id="EditProfileModalLabel">Profile details</h1>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>

            <div class="modal-body">
                <form method="post" enctype="multipart/form-data" asp-page-handler="EditProfile">
                    <div class="row">
                        <div class="col-7">
                            <input type="file" asp-for="NewPfp" accept=".jpeg, .jpg, .png" />
                        </div>
                        <div class="col-2 ms-3">
                            <input type="color" value="@Model.user.color" asp-for="NewColor" />
                        </div>
                    </div>
                    <div class="pt-5">
                        <input maxlength="50" class="form-control" placeholder="@Model.user.nickname" asp-for="NewNickname" />
                    </div>
                    <div class="form-control invisible"> </div><br />
                    <div class="d-flex justify-content-end mb-3">

                        <input type="submit" class="btn btn-success border-success col-3" data-bs-dismiss="modal" value="Confirm" />
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
