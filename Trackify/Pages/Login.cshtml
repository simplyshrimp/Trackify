@page
@model Trackify.Pages.LoginModel
@{
}
<div class="d-flex justify-content-center">
    <div class="card text-center" style="height: 600px; width: 600px;">
        <div class="align-content-center">
            {logo here}
            <h4 class="fw-bold"> Log in to Trackify</h4>
            <a class="form-control"></a>
            <a class="form-control"></a>
            <a class="form-control"></a>
            <div class="border"></div>
            <form method="post">
                <label class="fw-bold">Email or username</label>
                <textarea placeholder="Email or username"></textarea>
                <a class="form-control" href="">Continue</a>
            </form>
            <p> Don't have an account?<a>Sign up for Trackify</a></p>
        </div>
    </div>
</div>
